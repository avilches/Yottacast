# Plan 1: Estabilidad

Objetivo: eliminar los bugs encontrados en la revision 2026-06-12 y dejar la red de seguridad de tests en estado fiable.

**Backlog detallado**: `review-result-20260612193846.md` (raiz del proyecto). Ese fichero contiene cada bug con fichero:linea, reproduccion, impacto y fix sugerido. Este plan define el orden de ejecucion, agrupaciones y verificacion. Ejecutar este plan = ejecutar ese backlog en este orden.

## Preparacion

1. Leer `CLAUDE.md` y los docs del area que se toque en cada fase (lista en CLAUDE.md, seccion Documentacion). Como minimo: `docs/app-design.md`, `docs/search-sources.md`, `docs/search-clipboard.md`, `docs/ipc-daemon.md`, `docs/multi-platform.md`, `docs/user-settings.md`.
2. Verificar estado base: `cd Yottacast.Core.Tests && dotnet test` y `cd Yottacast.Ipc.Tests && dotnet test` deben pasar antes de empezar.
3. Trabajar por fases; cada fase termina con build verde y tests verdes. Commit por fase con mensaje breve descriptivo (sin coautores ni "Generated by").

## Fase A: criticos que crashean o bloquean (orden de urgencia)

1. **ShowCursorWin (Windows crash)**: `Yottacast/Services/WindowsAppHandler.cs:84-85`. Anadir `EntryPoint = "ShowCursor"` al DllImport.
2. **`new Uri("http://")`**: `Yottacast.Core/Search/Url/UrlSearch.cs:306` (TryNormalizeUrl). Validar con `Uri.TryCreate(url, UriKind.Absolute, out var u) && !string.IsNullOrEmpty(u.Host)`. Cubre tambien `UrlSearch.cs:47,120` y `ClipboardSearch.cs:97,130`. Anadir tests: query "http://", "https://", clipboard con "http://".
3. **Escaneo de apps fallido deja app sin ventana**: `Yottacast.Core/Search/Application/ApplicationSearch.cs:189-195`. try/catch/finally con `_readyTcs.TrySetResult()` en finally y log del error.
4. **PluginService async void**: `Yottacast.Core/Services/PluginService.cs:46-49`. Envolver el cuerpo de OnDirectoryChanged en try/catch con log (incluye el EnumerateFiles de la linea 55 y el Invoke de PluginsChanged de la 106).
5. **PluginService GetIcon race**: `PluginService.cs:25` vs `:98-102`. Lock en GetIcon o swap por referencia del diccionario completo (preferir swap, es el patron ya usado con `Plugins`).
6. **Linux tokens[0]**: `Yottacast.Core/Platform/LinuxPlatformProvider.cs:106-107`. Guard `if (tokens.Length == 0) return Task.CompletedTask;`.

## Fase B: criticos IPC

7. **Campo proto clipboard huerfano**: `Yottacast.Ipc/Proto/settings.proto:23`. PREGUNTAR AL USUARIO: opcion A reservar el tag (`reserved 9;`), opcion B exponer la configuracion nueva completa (enum de visibilidad + max entries/days + hotkey). La B es mas trabajo pero deja el daemon utilizable para la UI Swift.
8. **Round-trip ModeOnly a Always**: `Yottacast.Ipc/Mapping/SettingsMapper.cs:18,62`. Cambiar el bool del proto por enum/int32 y mapear directo. Coordinar con el punto 7 (mismo .proto).
9. Tras 7 y 8: reforzar `SettingsMapperTests` (ver Fase E, punto 20) para que el round-trip completo quede cubierto.

## Fase C: fire-and-forget y robustez del pipeline

10. **MainWindow**: `Yottacast/Views/MainWindow.axaml.cs:108` y `:355`. try/catch con `_logger`.
11. **GlobalSearch**: `Yottacast.Core/Search/GlobalSearch.cs:84-88`. Capturar Exception (no solo OCE) en el lambda del writer y loguear.
12. **UserDocumentSearch**: `Yottacast.Core/Search/UserDocuments/UserDocumentSearch.cs:262`. Catch general + `channel.Writer.TryComplete()` y `cts.Dispose()` en finally (hoy una excepcion no-OCE deja IsSearching colgado).
13. **FaviconCache / FileIconCache**: `FaviconCache.cs:36,51,74` y `FileIconCache.cs:63`. Mover la fase de disco dentro del try; en fallo limpiar `_started` para permitir reintento.
14. **ClipboardHistoryStore**: `Yottacast.Core/Search/Clipboard/ClipboardHistoryStore.cs:61,95,129-143` y `Yottacast/App.axaml.cs:161-162`. (a) await de LoadAsync antes de arrancar el monitor o merge en LoadAsync; (b) cancelar `_debounceCts` en Remove y serializar SaveAsync con SemaphoreSlim.
15. **Settings MaxEntries/MaxDays del clipboard sin cablear**: propagar `ClipboardHistoryMaxEntries`/`MaxDays` (`UserSettings.cs:80-81`) al store cuando cambian y aplicar recorte inmediato. Leer `docs/search-clipboard.md` antes; actualizar el doc si cambia el contrato.
16. **IPC streams**: serializar escrituras por watcher con un `Channel<T>` por suscriptor en `SettingsGrpcService`, `IconGrpcService`, `LifecycleGrpcService`; try/catch en los broadcasts async void con ToProto dentro del try; fix del fire-and-forget de `LifecycleGrpcService.cs:29`; lock alrededor de reset-execute-read de `_lastCopiedText` en `SearchGrpcService`.

## Fase D: menores de comportamiento

17. **UI**: Left/Right sin e.Handled (`MainWindow.axaml.cs:488-492`); capturas de hotkey simultaneas en SettingsWindow (guard + cancelacion cruzada); `KeyModifiers.Meta` hardcodeado (`MainWindow.axaml.cs:662,839`, exponer via AppHandler); drag con `#`/`%` (`DragDataFactory.cs:36`, UriBuilder).
18. **Core/Platform**: NameMatcher query de solo separadores (`NameMatcher.cs:45`); AppIconCache sin dedup en vuelo (replicar `_loading` de FileIconCache); InvalidateAll borra la version equivocada (`FileIconCache.cs:80-88`); Git Bash sin `-c` y entrada Windows Terminal muerta (`WindowsPlatformProvider.cs:196-216`); scan/watcher inconsistentes en Windows (`:45-73`); temporales huerfanos en macOS (`MacOsPlatformProvider.cs:238-239`); HistoryService.Save no atomico (`HistoryService.cs:85`); carrera Stop vs escaneo en ApplicationSearch (`:67-79,189-195`); ProcessRunner.IsSuccess y QuoteArg (`ProcessRunner.cs:47-57,83-84`).
19. **IPC menores**: AppDirectoriesChanged no disparado en UpdateSettings (`SettingsGrpcService.cs:50`); fallback "app" de ResultMapper a "unknown" (`ResultMapper.cs:72-77`); IDs de resultado con token de generacion (`SearchGrpcService.cs:46-52`); guard de PID con validacion de ProcessName (`Program.cs:19-32`).

## Fase E: tests y limpieza

20. **Tests vacios o falsos**: `LaunchHistoryTests.cs:77-95` (falta LoadAsync); `ProcessRunnerTests.cs:60-70` (assertar Cancelled); `ClipboardHistoryStoreTests.cs:59,126` (cambiar `>=` por `>`); `SettingsMapperTests.cs:50,94` (cobertura campo a campo del proto + colecciones + enum de visibilidad). Quitar el `<Protobuf>` duplicado de `Yottacast.Ipc.Tests.csproj:12-14` (CS0436).
21. **Tests que escriben en settings.json real**: localizarlos y aislarlos con directorio temporal.
22. **Codigo muerto**: `MacOsPlatformProvider.cs:316-360,507`; `ThemeService.cs:685-688,719-722`; `_longPressActivated` en MainWindow (PREGUNTAR: completar la logica de long-press o eliminar la variable).
23. **Constantes a AppDefaults**: lista completa en la seccion [ARQUITECTURA] del review-result. Una pasada unica.
24. **Gaps CLAUDE.md** (PREGUNTAR AL USUARIO antes de cada uno): busqueda web en modo emoji (implementar lo documentado o cambiar CLAUDE.md); paste en Linux; endpoint de UpdateChecker.

## Items dudosos (verificar antes de tocar)

Lista en la seccion "Dudosos" del review-result: CFRelease(NULL), SearchBox en modo Edit, Environment.Exit(0) en Cmd+Q, lazy init de discoveries, `_panels` de SystemSettingsSearch, DNS fire-and-forget, CTS sin Dispose, leaks de eventos en SettingsWindowViewModel. Para cada uno: confirmar que el escenario es alcanzable; si no, descartar y anotarlo.

## Verificacion final

- `dotnet build` de la solucion sin warnings nuevos (los CS0436 deben desaparecer).
- `cd Yottacast.Core.Tests && dotnet test` y `cd Yottacast.Ipc.Tests && dotnet test` verdes.
- Humo manual en macOS: abrir/ocultar con hotkey, teclear una URL completa caracter a caracter, modo emoji con flechas, copiar texto y verlo en clipboard history, abrir Settings y capturar ambas hotkeys.
- Actualizar los docs/ afectados (search-clipboard, ipc-daemon, user-settings) describiendo solo el comportamiento actual.
