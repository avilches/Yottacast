<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
<PackageReference Include="Jint" Version="3.1.0" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="9.0.0" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="9.0.0" />
  </ItemGroup>

  <!-- Download math.js on first build if not already present -->
  <Target Name="DownloadMathJs" BeforeTargets="BeforeBuild">
    <MakeDir Directories="$(MSBuildThisFileDirectory)Search/Calculator" />
    <Exec
      Command="curl -fsSL https://cdnjs.cloudflare.com/ajax/libs/mathjs/11.12.0/math.min.js -o &quot;$(MSBuildThisFileDirectory)Search/Calculator/math.min.js&quot;"
      Condition="!Exists('$(MSBuildThisFileDirectory)Search/Calculator/math.min.js')" />
  </Target>

  <!-- Download emoji data on first build if not already present -->
  <!-- To update to a newer version: delete Resources/emoji-data.json and rebuild -->
  <Target Name="DownloadEmojiData" BeforeTargets="BeforeBuild">
    <MakeDir Directories="$(MSBuildThisFileDirectory)Resources" />
    <Exec
      Command="curl -fsSL https://raw.githubusercontent.com/iamcal/emoji-data/refs/heads/master/emoji.json -o &quot;$(MSBuildThisFileDirectory)Resources/emoji-data.json&quot;"
      Condition="!Exists('$(MSBuildThisFileDirectory)Resources/emoji-data.json')" />
  </Target>

  <ItemGroup>
    <EmbeddedResource Include="Search/Calculator/math.min.js" />
    <EmbeddedResource Include="Resources/emoji-data.json" Condition="Exists('Resources/emoji-data.json')" />
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>Yottacast.Core.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

</Project>
