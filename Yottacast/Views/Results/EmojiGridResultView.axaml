<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:Yottacast.Core.ViewModels;assembly=Yottacast.Core"
             x:Class="Yottacast.Views.Results.EmojiGridResultView"
             x:DataType="viewModels:EmojiGridResultViewModel">
    <StackPanel Orientation="Vertical" Margin="4,8,4,8">
        <ItemsControl ItemsSource="{Binding Cells}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="viewModels:EmojiCellViewModel">
                    <Border Width="40" Height="40" CornerRadius="8" Margin="2"
                            Classes.emoji-selected="{Binding IsSelected}">
                        <TextBlock Text="{Binding Char}" FontSize="22"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,6,0,0" Spacing="2">
            <TextBlock Text="{Binding SelectedEmoji.Name}"
                       Foreground="{DynamicResource Theme.ItemTitle}"
                       FontSize="{DynamicResource Theme.FontSizeTitle}"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding SelectedEmoji.Category}"
                       Foreground="{DynamicResource Theme.ItemCategory}"
                       FontSize="{DynamicResource Theme.FontSizeSmall}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding SelectedEmoji.KeywordsText}"
                       Foreground="{DynamicResource Theme.ItemSubtitle}"
                       FontSize="{DynamicResource Theme.FontSizeSmall}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="400"/>
        </StackPanel>
    </StackPanel>
</UserControl>
